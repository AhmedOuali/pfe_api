{
  "name": "push_server",
  "version": "0.0.0",
  "description": "A chat example to showcase how to use `socket.io` with a static `express` server with `async` for control flow.",
  "main": "server.js",
  "repository": "",
  "author": "Mostafa Eweda <mo.eweda@gmail.com>",
  "scripts": {
    "format": "prettier --print-width 80 --no-semi --single-quote --trailing-comma es5 --write api/*.js"
  },
  "dependencies": {
    "async": "~0.2.8",
    "body-parser": "^1.18.2",
    "express": "~4.0.0",
    "fetch-cookie": "^0.7.0",
    "node-fetch": "^2.1.2",
    "node-storage": "0.0.7",
    "prettier": "^1.12.1",
    "socket.io": "~0.9.14",
    "web-push": "^3.3.0"
  }
}
